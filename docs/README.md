# docs

Every image the README uses, and the tour.

| File | What it is |
| --- | --- |
| `tour.mp4` | The tour of the app, two and a half minutes. Small enough to attach to a GitHub comment, which is the only way a video plays inside a README |
| `default-my-week.png` | My week, full window. The README's cover |
| `review.png` | The daily review, mid-step |
| `insights-dashboard.png` | The dashboard over a week, with the focus score |
| `composer.png` | The composer reading a date, a project, a priority and a tag |
| `thingstosettle-noestimates.png` | Things to settle |
| `deploying.md` | Uploading a build, and the two things to check afterwards |

The screenshots are exported from `todoistenhancedvisuals` and copied here, so
the README never points at a file outside the repository.
